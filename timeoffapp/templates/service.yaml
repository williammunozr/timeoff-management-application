apiVersion: v1
kind: Service
metadata:
  name: {{ include "timeoffapp.fullname" . }}
  labels:
    {{- include "timeoffapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timeoffapp.selectorLabels" . | nindent 4 }}
